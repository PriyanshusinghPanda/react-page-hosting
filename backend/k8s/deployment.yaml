apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploydash-backend
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deploydash-backend
  template:
    metadata:
      labels:
        app: deploydash-backend
    spec:
      serviceAccountName: deploy-backend-sa
      nodeSelector:
        kubernetes.io/hostname: nst-n1
      containers:
      - name: backend
        image: pandaincode/deploy-backend:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 7830
        env:
        - name: DEPLOY_STRATEGY
          value: "k8s"
        - name: PORT
          value: "7830"
        - name: MONGODB_URI
          value: "mongodb://mongodb-service:27017/deployDash"
        - name: STORAGE_ENDPOINT
          value: "http://s3-pandaincode.nstsdc.org/"
        - name: STORAGE_ACCESS_KEY
          value: "YOUR_MINIO_ACCESS_KEY"
        - name: STORAGE_SECRET_KEY
          value: "YOUR_MINIO_SECRET_KEY"
        - name: BUCKET_NAME
          value: "projects"
        resources:
          limits:
            cpu: "500m"
            memory: "1Gi"
          requests:
            cpu: "250m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: deploydash-backend
  namespace: default
spec:
  selector:
    app: deploydash-backend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 7830
  type: ClusterIP
